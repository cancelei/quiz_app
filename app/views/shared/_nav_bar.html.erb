<nav class="bg-blue-600 p-4 shadow-md">
  <div class="container mx-auto flex justify-between items-center">
    <%= link_to "QuizApp", root_path, class: "text-white font-bold text-xl" %>
    <div class="space-x-4">
      <% if logged_in? %>
        <div class="flex items-center space-x-4 gap-4">
          <%= link_to "Home", root_path, class: "text-white text-xl hover:underline" %>
          <%= link_to "Quizzes", quizzes_path, class: "text-white text-xl hover:underline" %>
          <% if current_user.admin? %>
            <%= link_to "Manage Quizzes", quizzes_path, class: "text-white text-xl hover:underline" %>
        <% end %>
        
        <%= button_to "Logout", session_path(Current.session), method: :delete, class: "text-white text-xl hover:underline" %>
      <% else %>
        <%= link_to "Login", sign_in_path, class: "text-white text-xl hover:underline" %>
        <%= link_to "Sign Up", sign_up_path, class: "text-white text-xl hover:underline" %>
      <% end %>
      </div>
    </div>
  </div>
</nav>
